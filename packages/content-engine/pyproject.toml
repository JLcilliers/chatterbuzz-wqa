[project]
name = "content-engine"
version = "0.1.0"
description = "AI content generation, publishing, tech SEO, GBP, keyword research, Asana sync, reporting"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
