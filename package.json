{
  "name": "chatterbuzz-seo-platform",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "setup": "bash scripts/setup.sh",
    "generate-types": "bash scripts/generate-types.sh",
    "web:dev": "pnpm --filter @chatterbuzz/web dev",
    "dashboard:dev": "pnpm --filter @chatterbuzz/dashboard dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0"
}
